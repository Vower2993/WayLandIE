/* Stub android/hardware_buffer.h for local syntax checking.
 * On CI, the real NDK header is used. This stub only exists so gcc
 * can resolve the include locally. */
#ifndef _STUB_AHB_H
#define _STUB_AHB_H
struct AHardwareBuffer;
typedef struct AHardwareBuffer AHardwareBuffer;
#endif
