# Gradle / Android Studio
.gradle/
build/
local.properties
*.iml
.idea/
captures/
.cxx/

# Native build artifacts
app/.cxx/
app/build/

# Bionic bridge deps — built by tools/build-bionic-bridge.sh, cached by CI.
# Don't commit (1MB of static .a files that rebuild in ~5 min).
app/src/main/cpp/bionic-libs/

# Keystore (don't accidentally commit a release keystore)
!app/keystore/debug.keystore
*.jks
*.keystore
!debug.keystore

# OS
.DS_Store
Thumbs.db
